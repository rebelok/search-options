/**
 * Created by user on 03.07.2015.
 */

import React, { Component } from 'react';

export default class SearchOprions {
    var store = [],map;
    init() {
        for (let i = 1; i < 11; i++) {
            store.push({Name: "so" + i, Id: i, State: 0});
        }

    }


}
